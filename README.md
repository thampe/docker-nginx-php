# nginx + PHP FPM Docker container

Alpine-based PHP 8.5 container for building Magento 1 and 2 images. It uses [Supervisord](http://supervisord.org/) to start nginx, PHP-FPM, and a cron daemon.

Container images can be downloaded from [Docker Hub](https://hub.docker.com/r/thampe/nginx-php/tags).
